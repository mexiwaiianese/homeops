import {NextResponse} from "next/server";
import {getAuthedContext} from "@/lib/backend";
import {explainVendorEligibility} from "@/lib/vendors";
import {vendors as demoVendors} from "@/lib/vendor-demo";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {supabase,user,organizationId}=await getAuthedContext();const {id}=await params;if(!supabase)return NextResponse.json({mode:"demo",vendors:demoVendors.map(v=>({...v,eligibility:explainVendorEligibility(v)}))});if(!user||!organizationId)return NextResponse.json({error:"Authentication required"},{status:401});const {data:req}=await supabase.from("maintenance_requests").select("home_id,service_category_id").eq("id",id).eq("organization_id",organizationId).single();if(!req)return NextResponse.json({error:"Request not found"},{status:404});const {data,error}=await supabase.from("vendors").select("*,vendor_credentials(*),vendor_services(*),vendor_owner_preferences(*),vendor_property_preferences(*)").eq("organization_id",organizationId).order("name");if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({mode:"live",vendors:(data??[]).map(v=>({...v,eligibility:explainVendorEligibility(v,{homeId:req.home_id,serviceCategoryId:req.service_category_id})}))});}
